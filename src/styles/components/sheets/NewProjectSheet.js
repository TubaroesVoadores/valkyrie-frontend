import { createStyles } from '@mantine/core';

export const useNewProjectSheetStyles = createStyles(() => ({
  modalContent: {
    display: 'flex',
    flexDirection: 'column',
    padding: '1rem',
  },
}));
